name: CI

on:
  push:
    branches: [main, "agent/**"]
  pull_request:

permissions:
  contents: read

jobs:
  frontend-quality:
    name: Frontend quality (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.9.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Frontend lint
        id: frontend_lint
        continue-on-error: true
        run: pnpm lint > lint.log 2>&1
      - name: Upload lint diagnostics
        if: steps.frontend_lint.outcome == 'failure'
        uses: actions/upload-artifact@v6
        with:
          name: frontend-lint-${{ matrix.os }}
          path: lint.log
          if-no-files-found: error
      - name: Enforce lint result
        if: steps.frontend_lint.outcome == 'failure'
        run: exit 1
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm test:integration
      - run: pnpm build

  rust-quality:
    name: Rust core (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.9.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: Install Rust components
        shell: pwsh
        run: |
          rustup toolchain install stable --profile minimal --component rustfmt,clippy
          rustup default stable
      - name: Rust format
        id: rustfmt
        continue-on-error: true
        run: cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --all -- --check > rustfmt.log 2>&1
      - name: Upload rustfmt diagnostics
        if: steps.rustfmt.outcome == 'failure'
        uses: actions/upload-artifact@v6
        with:
          name: rustfmt-windows
          path: rustfmt.log
          if-no-files-found: error
      - name: Enforce rustfmt result
        if: steps.rustfmt.outcome == 'failure'
        run: exit 1
      - run: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --all-features
      - run: cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets --all-features -- -D warnings

  windows-bundle:
    name: Windows NSIS bundle and size gate
    runs-on: windows-latest
    needs: [frontend-quality, rust-quality]
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.9.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install Rust and Tauri CLI
        shell: pwsh
        run: |
          rustup toolchain install stable --profile minimal
          rustup default stable
          cargo install tauri-cli --version 2.11.2 --locked
      - run: pnpm dist:win
      - name: Enforce lightweight installer
        shell: pwsh
        run: |
          $installer = Get-ChildItem -LiteralPath apps/desktop/src-tauri/target/release/bundle/nsis -Filter '*.exe' | Select-Object -First 1
          if ($null -eq $installer) { throw 'Tauri NSIS installer not found.' }
          $limit = 40MB
          if ($installer.Length -ge $limit) { throw "Installer is $($installer.Length) bytes; limit is $limit bytes." }
          Write-Host "RemoteDeck installer: $([math]::Round($installer.Length / 1MB, 2)) MiB"
      - uses: actions/upload-artifact@v6
        with:
          name: RemoteDeck-2.0.0-alpha.1-windows-x64
          path: apps/desktop/src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
